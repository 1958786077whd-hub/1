name: Build FreeTube iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/build-freetube-ios.yml'
      - 'README.md'

permissions:
  contents: read

jobs:
  build-ios:
    name: Build Invidious iOS IPA
    runs-on: macos-26
    timeout-minutes: 60

    steps:
      - name: Checkout builder repository
        uses: actions/checkout@v4

      - name: Clone Yattee 1.x with Invidious support
        run: |
          git clone --depth 1 --branch v1.5.2-257 https://github.com/yattee/yattee.git upstream
          cd upstream
          echo "UPSTREAM_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Resolve Swift packages
        working-directory: upstream
        run: |
          xcodebuild \
            -resolvePackageDependencies \
            -project Yattee.xcodeproj \
            -scheme 'Yattee (iOS)'

      - name: Build iOS app without code signing
        working-directory: upstream
        run: |
          set -o pipefail
          xcodebuild \
            -project Yattee.xcodeproj \
            -scheme 'Yattee (iOS)' \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$RUNNER_TEMP/FreeTubeDerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            DEVELOPMENT_TEAM='' \
            build | tee "$RUNNER_TEMP/xcodebuild.log"

      - name: Locate built app
        run: |
          APP_PATH=$(find "$RUNNER_TEMP/FreeTubeDerivedData/Build/Products" -maxdepth 4 -type d -name 'Yattee.app' | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo 'Yattee.app was not found after build.'
            find "$RUNNER_TEMP/FreeTubeDerivedData/Build/Products" -maxdepth 5 -print || true
            exit 1
          fi
          echo "APP_PATH=$APP_PATH" >> "$GITHUB_ENV"
          echo "Built app: $APP_PATH"

      - name: Prepare sideload-friendly FreeTube app
        run: |
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName FreeTube" "$APP_PATH/Info.plist" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Add :CFBundleDisplayName string FreeTube" "$APP_PATH/Info.plist"

          # SideStore/AltStore can re-sign the main app. Strip extensions so a
          # free Apple ID does not need additional App IDs for non-core targets.
          rm -rf "$APP_PATH/PlugIns"

          echo "Display name: $(/usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$APP_PATH/Info.plist")"
          echo "Bundle ID: $(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "$APP_PATH/Info.plist")"
          echo "Minimum iOS: $(/usr/libexec/PlistBuddy -c 'Print :MinimumOSVersion' "$APP_PATH/Info.plist")"

      - name: Package unsigned IPA
        run: |
          rm -rf "$RUNNER_TEMP/Payload" "$RUNNER_TEMP/FreeTube.ipa"
          mkdir -p "$RUNNER_TEMP/Payload"
          cp -R "$APP_PATH" "$RUNNER_TEMP/Payload/FreeTube.app"
          cd "$RUNNER_TEMP"
          /usr/bin/zip -qry FreeTube.ipa Payload
          ls -lh FreeTube.ipa

      - name: Write build metadata
        run: |
          cat > "$RUNNER_TEMP/build-info.txt" <<EOF
          App: FreeTube iOS (Yattee 1.x)
          Backend support: Invidious / Piped
          Upstream: yattee/yattee
          Upstream branch: v1.5.2-257
          Upstream commit: $UPSTREAM_SHA
          Build mode: unsigned / sideload
          Runner: macos-26 / Xcode 26
          EOF

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: FreeTube-Invidious-iOS-unsigned
          path: |
            ${{ runner.temp }}/FreeTube.ipa
            ${{ runner.temp }}/build-info.txt
          if-no-files-found: error
          compression-level: 0
          retention-days: 30

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: FreeTube-Invidious-build-log
          path: ${{ runner.temp }}/xcodebuild.log
          if-no-files-found: ignore
          retention-days: 7
